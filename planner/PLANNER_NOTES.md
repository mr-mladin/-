# Планер — карта проекта и договорённости

> **Для Claude (и владельца): читать в начале каждого чата по планеру, обновлять при
> значимых решениях.** Это «долгая память» между чатами — я не помню прошлые сессии,
> но помню этот файл + комментарии в коде. Дополняет корневой `CLAUDE.md` (там общий
> стек и git-workflow), здесь — внутренности именно планера, решения и грабли.

## ⏩ Передача в новый чат (обновлено 2026-06-24, сессия №7)

После выкатки этой сессии: ассеты `styles.css?v=125`, `main.js?v=133`, `Planner.js?v=133`,
service worker `v6`.
Деплой: push в `main` → GitHub Actions `pages.yml` → GitHub Pages. **Грабли деплоя:** в
эфемерном контейнере локальная ссылка `origin/main` бывает УСТАРЕВШЕЙ, и обычный
`git fetch origin main` её НЕ обновляет (только FETCH_HEAD). Истину брать через
`git ls-remote origin refs/heads/main`; чинить
`git fetch origin +refs/heads/main:refs/remotes/origin/main`.

**Сделано в этой сессии:**
- **Зона «весь день» — «просвет сверху» убран (в проде).** Зона ОСТАЁТСЯ в сетке (закреплена,
  непрозрачный фон). Фикс: у карточки-сетки `.planner-grid-scroll` убрали верхний отступ
  (`padding-top` был 6px → 0). Из-за него закреплённая зона залипала на ~3px НИЖЕ верха
  карточки, и в этой щели сверху просвечивали уезжающие события. Без отступа зона прижата к
  самому верху — щели нет. Подробно — раздел «Зона весь день».
- **Тупик, НЕ повторять:** выносил зону наружу отдельной прозрачной полосой
  (`.planner-allday-bar` между датами и сеткой) — выкатил, владелец ОТКЛОНИЛ, вернул в сетку.
- **Большой аудит кода + пакет исправлений (в проде).** 6 файлов: `lib` (NaN-время,
  строгий разбор `hhmmToMin`), `store` (фоновая перечитка не затирает правки во время
  загрузки → задача не «прыгает назад»; откат автопереноса при сбое; сброс `rolledFor`
  при смене юзера), `components` (замок от дубля задачи, очистка времени, `min` у «до»,
  aria), `sw` (версии кэша `VERSION`, офлайн-фолбэк навигации, не кэшируем opaque),
  `Planner` (мёртвый код `trayTasks`/`rowToItem`/`countArea`/`dndGeomRef`, guard `yToMin`),
  `styles` (`:focus-visible`, тосты z-index 90 поверх формы, тёмная тень тостов, iOS-зум).
  **Отложено (с пометками):** хрупкие зоны drag/свайп (guard `runDaySwipe`, rAF в `land`,
  флик трекпада — только с тестом на iPhone, перед/в рамках переборки свайпа); часть CSS
  (тёмные тени всплывашек, тач-цели 44px на мобайле, контраст `--text-mute`, мёртвый CSS
  `.sheet-*`/`.tray-*`/`.dt-*`); CSP; поиск (debounce + навигация стрелками); сброс пароля.
  **Порция 2 (в проде):** повтор «каждый месяц 31-го» → последний день месяца, как Apple
  (`occursOn`, `Math.min(anchor, daysInMonth)`); поиск — навигация ↑/↓/Enter + дебаунс 120мс +
  мемоизация; CSS-полировка (тёмные тени всплывашек, тач-цели 40px в шапке, контраст
  `--text-mute` #5f6878, подсветка активного результата). **Осталось:** ориентация PWA; CSP;
  сброс пароля; вычистка мёртвого CSS (`.sheet-*`/`.tray-*`/`.dt-*`); защиты в drag/свайп (тест).
  **Порция 3 (в работе, сессия №3):** убрана видимая рамка фокуса (`:focus-visible{outline:none}`,
  v=123, по просьбе владельца). Перенос задач — «живее» (v=124): анимированный подхват через
  отдельное свойство `scale` (pop .985→1.04, не мешает живому translate), глубже тень поднятой
  карточки, мягкий возврат scale+тени на доезде. Поведение НЕ менялось (решение: «сделать плавнее»).
  **Свайп — переборка ПОРЦИЯМИ с тестом владельца** (его решение). Диагноз: сетка дня листает через
  JS-rAF `animateTo` (effect ~458) → «крючок»; строка дат и неделя/месяц уже на CSS-transition-доводке
  (`weekBarCommit`/`daySwipeCommit`) — эталон. План: (1) строка дат на трекпаде `onWeekStripWheel` —
  добавить «быстрый смах» — СДЕЛАНО (v=129): порог 16%→12% + флик по ПИКОВОЙ скорости жеста
  (`weekWheelPeakVxRef`, порог 0.5 px/мс, тюнится по тесту владельца);
  (2) сетку дня перевести с `animateTo` на CSS-доводку как `weekBarCommit` (перехват жеста в доводке —
  читать живую позицию из computed-transform). ОСТОРОЖНО: в дневной свайп вшиты перенос 2-м пальцем,
  зум, edge-меню, keepScroll.
  **Сессия №4 (2026-06-22): свайп и зум разведены, физика дня перестроена.** Активный однопальцевый
  `runDaySwipe` хранится в `dayTouchGestureRef`; появление второго пальца немедленно отменяет
  свайп, возвращает ленту в центр БЕЗ смены даты и только затем запускает вертикальный щипок.
  Та же взаимная блокировка добавлена для Ctrl+колеса/Safari gesture на десктопе. Создание или
  подъём задачи теперь тоже отменяет обычный свайп без случайного коммита соседнего дня.
  Старый JS-rAF `animateTo` удалён: палец/трекпад ведёт transform напрямую, после отпускания
  работает одна CSS-доводка `cubic-bezier(.18,.82,.22,1)` с длительностью по остатку пути и
  скорости. Решение о перелистывании учитывает фактическое и прогнозируемое положение.
  Новое касание прерывает доводку, считывает живой computed-transform и продолжает с него —
  без паузы/«крючка». Трекпад получил axis-lock и реальную оценку скорости. Кэш: `main.js?v=130`,
  `Planner.js?v=130`, SW `v3`.
- **Сессия №5 (2026-06-22): исправлено серийное листание.** Реальная причина паузы на Mac:
  браузер продолжает присылать инерционные `wheel`-события до 1–2 секунд, а карусель ждала
  полного окончания потока и оставалась упёртой в соседнюю панель. Теперь жест имеет фазы
  `drag/drain`: остаточная инерция отсекается, новый импульс немедленно завершает прошлый
  шаг, синхронно меняет дату и начинает следующий свайп с центра. Та же схема применена к
  десктопной строке дат. На таче сетка дня использует порог, скорость и CSS-кривую мобильной
  строки дат; новый свайп во время доводки не обнуляется сменой даты. Проверено сериями:
  5 быстрых свайпов сетки = 5 дней, 4 свайпа строки дат = 4 недели, включая смену направления.
  Кэш: `main.js?v=131`, `Planner.js?v=131`, SW `v4`.
- **Сессия №6 (2026-06-22): десктопный свайп переведён на нативную карусель.** Ручной
  `wheel`-движок для сетки дня и верхней строки дат на десктопе удалён. Теперь обе области
  используют горизонтальный `overflow` + CSS `scroll-snap`: браузер сам ведёт панель вместе
  с пальцами, различает направление жеста и начинает доводку только после завершения жеста.
  На современных браузерах дата меняется по `scrollend` (то есть после release); таймер
  оставлен только как совместимый запасной путь для старых браузеров без `scrollend`.
  Десктопная карусель содержит 9 панелей (±4 дня/недели), поэтому серия быстрых свайпов
  не упирается в край до обновления даты. После коммита окно без анимации возвращается
  в центральную панель до отрисовки — визуального перескока нет. Мобильный ручной свайп
  не менялся: там по-прежнему 3 панели и прежняя проверенная физика.
  Проверено: 4 быстрых свайпа вправо/влево = ровно ±4 дня; 4 свайпа строки дат = +4 недели;
  центрирование стабильно, ошибок консоли нет. Кэш: `styles.css?v=125`, `main.js?v=132`,
  `Planner.js?v=132`, SW `v5`.
- **Сессия №7 (2026-06-24): полировка нативной десктопной карусели.** Логику свайпа не
  переписывали. Добавлен накопительный axis-lock для десктопной сетки дня и верхней строки
  дат: вертикальный/диагональный жест больше не должен случайно уводить scroll-snap в соседний
  день/неделю, но горизонтальный жест может «перебить» небольшой начальный вертикальный шум.
  На десктопе ближайшие соседние панели дня (`prev`/`next`) теперь отрисованы заранее, чтобы
  первый кадр горизонтального свайпа не стартовал с пустой/холодной панели. Дальние панели
  ±2..±4 по-прежнему подгружаются только на время `peek`. Мобильный путь не менялся.
  Проверено на локальном мок-стенде: диагональный жест по сетке/строке дат дату не меняет;
  3 горизонтальных свайпа сетки = +3 дня; 2 свайпа строки дат = +2 недели; горизонтальный
  жест после маленького вертикального шума срабатывает; мобильный брейкпоинт остаётся 3
  панели без `native-carousel`. Кэш: `styles.css?v=125`, `main.js?v=133`,
  `Planner.js?v=133`, SW `v6`.

**Открытый вопрос / в работе:**
1. **Свайп — проверить нативную десктопную карусель владельцем на реальном Mac.** Эталон =
   мобильная строка дат внутри приложения. На десктопе решение о странице теперь принимает
   браузерный scroll-snap после release, а не JS по таймеру.
   ОСТОРОЖНО: в дневной свайп вшиты перенос задач 2-м пальцем, зум, edge-меню, keepScroll.
   Ориентир ощущения — Apple Календарь.

Раньше готово и в проде: новая форма задачи (Apple-стиль), компактная строка дат с каруселью.

## Стандарт доводки (планка) — применять к КАЖДОМУ новому элементу

Проект на уровне Apple. Новое делаем сразу презентабельно, не «лишь бы работало».
Перед коммитом нового UI прогнать чек-лист:
- **Согласованность**: размеры/отступы/радиусы/шрифты — как у соседних элементов; через
  переменные (`--glass`, `--radius-*`, тени), а не случайные числа.
- **Стиль проекта**: копировать существующие паттерны (капсулы, переключатели, стекло,
  emoji-picker), а не изобретать с нуля.
- **Состояния**: hover / active / selected / disabled — везде, где уместно.
- **Без inline-«тяп-ляп»**: стили в CSS-классах, не длинной строкой в разметке.
- **Мелочи**: выравнивание; тень/контур не обрезаются; тёмная тема; иконка/подпись понятны;
  анимация плавная; единый язык с остальным UI.
- Глаз владельца всё равно нужен (я не вижу результат вживую), но криво-по-умолчанию — нельзя.

## Где что лежит (код переплетён — это важно!)

Области интерфейса **не разделены по файлам**. Почти всё — в двух больших файлах:
- `Planner.js` (~2,5K строк) — вся логика и разметка: сетка дня, «весь день», боковое
  дерево, карточки, жесты (перенос, свайпы, зум), клавиатура, недельный/месячный вид.
- `styles.css` (~1,2K строк) — все стили.
- `store.js` — состояние + CRUD (оптимистичные операции, undo/redo, загрузка).
- `lib.js` — клиент Supabase, сетевой слой, даты, повторения, иконки, `errHint`.
- `components.js` — формы (TaskEditor, AuthForm, модалки), `dbHint`/`authHint`.
- `sw.js` — service worker. `index.html` — boot, importmap, версии ассетов.

Вывод: чат «только карточки» всё равно заденет сетку и панель — они в одном коде.
Делить работу лучше по **темам/задачам**, а не по «областям экрана».

## Ключевые механики (где легко сломать)

### Перенос задачи (drag) — самое хрупкое
- Палец/курсор поднимает задачу → рисуется **плавающая копия** (`.tl-lift-overlay`,
  fixed поверх всего), оригинал в сетке прячется (`.lift-hidden`, `visibility:hidden`,
  `transition:none`).
- При отпускании — **«доезд»** (`.landing`): копия едет к слоту за ~0.2с, затем `commit()`
  (reschedule), затем на **следующем кадре** (`requestAnimationFrame`) — `done:true`:
  оригинал показывается уже на месте, копия исчезает. rAF нужен, чтобы оригинал не
  «доезжал» с анимацией top (иначе откат-назад-потом-вперёд).
- `setLiftDrag` в move — **ПРЯМОЙ, без троттлинга.** Троттлинг (rafThrottle) для переноса
  блока вызывал дёрганье «доезда» при малом переносе: отставание = заметный «откат к
  началу» (при большом переносе терялось на фоне движения — отсюда «дёргается без
  перескока, норм с перескоком»). `rafThrottle` остался только для ресайза/панели/дерева.
- Мобильный путь — `onBlockTouch`, десктоп — `startBlockLift`. Логика дублируется.

### Оптимистичные обновления (store.js)
- `updateTaskOptimistic` применяет правку сразу, в фоне пишет в БД. **НЕ перезаписывает
  state серверным ответом** — при быстрых правках (drag, Shift+стрелки) поздний ответ
  устаревает и кидает задачу назад. Ошибку откатывает на `prev`.
- `create`/insert **не повторяем** при сбое (дубли). update/delete — идемпотентны.

### Сетевой слой (lib.js)
- `fetchWithTimeout`: тайм-аут 20с + **автоповтор идемпотентных** (GET/PATCH/DELETE) при
  abort/сетевом сбое. Уважает signal отмены от supabase. Это лечит «нет связи»/«AbortError»
  при рабочем интернете (одиночные обрывы).

### Service worker (sw.js)
- CDN (esm.sh) — cache-first. Свои файлы — **network-first + кэш-фолбэк + тайм-аут 7с**:
  на плохой сети приложение стартует из кэша, а не висит белым/крутилкой. Свежесть кода
  сохраняется (сеть в приоритете).

### Чекбокс и плотность блоков
- Плотность: `height >= 44 ? "" : >= 24 ? "compact" : "mini"`.
- **Чекбокс ВСЕГДА на уровне названия (у верха, `top:7`), НЕ по центру блока.** Название
  и время тоже прижаты к верху во всех режимах — чекбокс и текст на одной линии, позиция
  не зависит от высоты (не прыгает при зуме).
- В compact/mini контент **уменьшается, чтобы помещаться** (чекбокс 17/12px, шрифт мельче).
  **НЕ использовать `overflow:hidden` на `.tl-event`** — он режет тень и чекбокс.
- Выполненная задача: шарик-чекбокс опускается вниз (`--drop = height - 34`, от верха).

### Зона «весь день» закреплена (десктоп)
- **«Просвет сверху» — ФИКС (сессия №2):** у карточки `.planner-grid-scroll` убрали верхний
  отступ (`padding: 6px 0 0` → `padding: 0`). Из-за 6px-отступа закреплённая зона залипала на
  ~3px НИЖЕ верха карточки (sticky `top` отсчитывается от content-box, т.е. ВНУТРИ padding), и
  в этой щели сверху просвечивали уезжающие события. Без отступа зона прижата к самому верху —
  щель исчезла. **Полосу-вынос (`.planner-allday-bar`) НЕ возвращать** — владелец её отклонил.
- В разметке `.allday` обёрнута в `.allday-stick` (см. `.tl-pane` в Planner.js). На десктопе
  (`@media min-width:861px`) липкой делаем именно **ОБЁРТКУ**: `.allday-stick {
  position:sticky; top:0; z-index:9; background:surface }` — full-width, БЕЗ transform.
  `.allday` внутри по-прежнему центрируется и сдвигается под сетку (`position:relative;
  left:32px; max-width:720; margin:auto`). При скролле сетки зона остаётся сверху, часы
  уезжают под неё.
- **Грабли (важно!):** ПЕРВЫЙ заход — `position:sticky` + `transform:translateX(32px)` прямо
  на `.allday` — НЕ залипал. Причина: **self-transform на sticky-элементе ломает залипание
  в Safari** (создаёт containing block). Поэтому transform убрали, сдвиг 32px оставили на
  `.allday` через `left` (relative, не sticky), а sticky вынесли на обёртку без transform —
  как рабочий `.week-head`. `left:32px` нельзя ставить на сам sticky-элемент: там `left`
  стал бы горизонтальным порогом залипания.
- Скролл к «сейчас» не ломается: `off` в логике учитывает высоту зоны, «сейчас» встаёт на
  y≈120 — ниже закреплённой зоны.
- **ГЛАВНОЕ про просвечивание (после 4 провалов фрост-подходов):** `backdrop-filter` НЕ
  работает внутри элемента с `transform`-предком (баг WebKit; лента дня `.tl-track` на
  transform). Поэтому ЛЮБОЙ фрост на `.allday-stick`/`.week-head` был «мёртвым» — слой не
  создавался, сквозь полупрозрачный фон видны события. РЕШЕНИЕ: фон зоны
  **ПОЛНОСТЬЮ непрозрачный** `var(--surface)`, БЕЗ backdrop-filter, + `isolation:isolate` на
  `.tl-pane` (z-index зоны авторитетен → рисуется над сеткой). НЕ возвращать backdrop-filter
  на зону! Если и это просвечивает — значит isolation не победил порядок отрисовки, и нужен
  СТРУКТУРНЫЙ фикс: вынести `.allday-stick` ИЗ `.tl-track` (из-под transform) в сам
  `.planner-grid-scroll` (тогда баг не действует; минус — зона не «едет» с днём при свайпе).
- **Просвечивание сетки сквозь зону** (долго мучились): причина — Safari-квирк, sticky внутри
  `transform`-трека НЕ поднимается на свой слой по z-index, события рисуются поверх фона зоны.
  Лечится `backdrop-filter` на обёртке `.allday-stick` (выносит на отдельный КОМПОЗИТНЫЙ слой
  поверх сетки — как `.week-head`, который по той же причине не просвечивает). **КРИТИЧНО: фон
  обёртки должен быть ПОЛУПРОЗРАЧНЫМ** (`color-mix(... 85%, transparent)`), НЕ полностью
  непрозрачным! При непрозрачном фоне браузер «выкидывает» backdrop-filter (он невидим) →
  композитный слой не создаётся → квирк возвращается, сетка просвечивает. С полупрозрачным
  фоном фильтр активен → слой поднимается. (Сначала поставил непрозрачный — не сработало.)
  НЕ `transform` на самой sticky (ломает залипание). Усиления: фон 92% + `top:-3px` (шов
  сверху) + **`isolation: isolate` на `.tl-pane`** — свой контекст наложения, чтобы z-index
  зоны был авторитетным и она рисовалась НАД сеткой (главный обход Safari-квирка; одного
  backdrop-filter не хватало). `.allday-stick` теперь в БАЗЕ (закреплён и на телефоне тоже),
  ручка шторки перенесена ВНУТРЬ `.allday-stick` (на телефоне закрепляется вместе с зоной).
  Во время свайпа дня blur зоны снимаем (`.planner-grid-scroll.swiping .allday-stick`).
- На мобильном (`@media max-width:860px`) `.allday-stick` без правил = обычный блок-обёртка
  (не sticky); зона остаётся шторкой `--adh` с ручкой, как было. Закрепить на телефоне —
  отдельная задача (sticky и для ручки).

### Зум сетки
- Мобильный — **только вертикальный двупальцевый щипок** (пальцы разнесены по вертикали).
  Десктоп — любой щипок / Ctrl+колесо. Класс `.zooming` на время жеста **снимает blur
  стекла** (иначе тяжело) и отключает доводки внутренних элементов.

### Стекло (матовость)
- Переменные `--glass` (заливка), `--glass-blur` (размытие) — **единые во всех разделах**
  (сетка, «весь день», дерево, чип подзадач, неделя, месяц). Тёмная тема — заливка выше.
- **`backdrop-filter` дорог при ДВИЖЕНИИ.** При свайпе дней карусель двигает 3 панели,
  на каждой — стекло у задач; перерисовка размытия ×3 за кадр → рывки/мелькание (сильнее
  «на холодную», сразу после загрузки). Лечится снятием blur на время жеста: класс
  `.swiping` (привязан к `peek`) на `.planner-grid-scroll` убирает `backdrop-filter` и
  доводки — как `.zooming` для зума. **Правило: любую анимацию/перенос поверх стекла
  сопровождать снятием blur на время движения.**

### Тень блока
- Единая для `sel` / `lifted` / `landing`, мягкая: `0 5px 16px rgba(2,6,23,0.15)`.

### Форма задачи (TaskEditor)
- Десктоп — **поповер-облачко СБОКУ** от задачи (`edPopover`: `.ed-float-back.popover`,
  `.ed-card` position fixed, координаты из JS-useLayoutEffect, фон overlay прозрачный —
  сетку НЕ двигаем, задачу не перекрываем). Мобильный — лист снизу / привязанный оверлей
  (`edAnchorMobile`), в сетку класть нельзя (user-select гасит клавиатуру в PWA).
- **«Жидкое стекло»** (`.ed-card`): полупрозрачная (но НЕ слишком — `--glass-card` ~88%/82%
  surface, владелец просил плотнее) заливка + `backdrop-filter` (`--glass-card-blur`),
  **БЕЗ рамки** (`border:0`) — рамка давала видимую линию у основания хвостика; контур даёт
  мягкая тень + верхний блик (`inset 0 1px 0 --glass-hi`), радиус 22px. Внутренние блоки
  (`.ed-headgroup`, `.ed-datebox`) — «слегка серый» фрост `--glass-panel`; пилюли-контролы
  внутри (`.ed-projdot`, `.ed-datepill`, `.ed-timepill`) на `var(--surface)` — выделяются.
  Живое имя: форма зовёт `onLiveTitle` → `liveEdit` в Planner → карточка в сетке (`tl-title`)
  сразу показывает новое название.
- **Верхняя строка** `.ed-top` (как в Apple): слева ✕ (`.ed-icbtn.ed-cancel`, `Icon.close`),
  по центру иконочный переключатель `.ed-typeseg` Задача(`Icon.checklist`)/Событие(`Icon.calendar`),
  справа ✓ (`.ed-icbtn.ed-save`, акцентный, `Icon.check`). `justify-content:space-between` +
  симметричные ✕/✓ → сегмент ровно по центру. Иконки центрируются строго (`line-height:0` +
  `.ico{display:block}` — убирают baseline-зазор inline-svg). Переключение **анимировано**:
  белая пилюля `.ed-typeseg-ind` скользит `translateX` (класс `.on-event` на сегменте).
  Иконка «Задача» `Icon.checklist` — верхний пункт залитый кружок + белая галочка (чтобы не
  «слипался» на 16px), нижний контурный. Степпер проекта `.ed-projdot` — крупнее.
- Оформление события (полоса слева/фон) — **свёрнуто по умолчанию** в строку «Оформление»
  (`styleOpen`), раскрывается по клику (как «Заметка»/«Подзадачи»). Это НЕ баг-пропажа —
  так задумано, чтобы форма не перегружалась 9 плашками сразу при выборе «Событие».
- **Вид под Apple Календарь**: шапка `.ed-head` = название (`textarea`, переносится,
  `field-sizing:content`, ~19px) + справа пилюля-**степпер** проекта (`.ed-projdot`:
  цветная точка + иконка `Icon.stepper()` вверх-вниз; клик → меню выбора).
- **Дата/время/повтор — одна сгруппированная карточка** `.ed-datebox` (как inset-group в
  iOS): строки `.ed-daterow` со светлым фоном и тонкими разделителями (`border-top`).
  Внутри — белые пилюли-контролы на `var(--surface)`: дата `.ed-datepill` (текст
  `medDate` = «12 июня 2026» + прозрачный `<input type=date>` поверх) и `.ed-timepill`
  (нативный `<input type=time>`). Строки: **Весь день** (iOS-тумблер `.ed-switch`,
  зелёный #34c759; вкл → `start_min=null`, показывает одну строку «Дата»; выкл →
  `changeStart("09:00")`, показывает «Начало/Конец»), **Начало/Конец** (дата+время),
  **Повтор** (`.ed-rep-val` + меню `RECUR_OPTIONS`), **Завершить** (дата «до», когда есть
  повтор). Порядок формы: верхняя строка → **блок «Название+Заметка»** → [Оформление
  события] → карточка даты → Подзадачи → Удалить. Старые `.ed-when*`/`.ed-rep-btn`/
  `.ed-until` удалены.
- **Название + Заметка — один блок** `.ed-headgroup` (как «второе поле» в Apple): строка
  названия (`.ed-head`) со степпером проекта справа, ниже — заметка (`.ed-notes`,
  прозрачная, через `border-top`-разделитель). Фон даёт группа, не само поле.
- **Позиционирование облачка** (десктоп, `edPopover`): по реальному rect блока в сетке —
  задача в верхней половине экрана → форма СНИЗУ, в нижней → СВЕРХУ, узкий блок-колонка
  (пересечение) → СБОКУ. Координаты (`left/top`) считает `useLayoutEffect` в Planner.js.
- **Хвостик облачка УБРАН** (после ~6 итераций — владелец решил убрать). Почему не вышло:
  CSS-хвостик делали `::after` с `mask` (форма треугольника) + `backdrop-filter` — но
  масочный псевдоэлемент в браузере НЕ подхватывал тон стекла карточки (выглядел отдельной
  нашлёпкой), а непрозрачный вариант сливался с фоном в тёмной теме. Вердикт: облачко просто
  всплывает у задачи без хвостика (чисто и обычно так и делают). Если снова захотят хвостик —
  это, видимо, требует не `::after`+mask, а реально единой формы (один элемент с обоими —
  напр. SVG-фон карточки целиком, либо `clip-path` с выступом, но он не тянется по высоте).
  Иконка `Icon.checklist` (как в референсе Apple Reminders): верхний кружок ЗАЛИТ акцентом
  (`fill=currentColor`), галочка — цвета фона (`.ed-task-check{stroke:var(--surface)}`, как
  вырезанная); нижний кружок — контурный. «Тонкость» контура — пропорция обводка/кружок (НЕ
  размер на экране!): кружки крупные (r3.4) + тонкая обводка `.ed-typebtn .ico{stroke-width:1.2}`.
  Подбирали офлайн-растеризацией (sharp), в т.ч. проверяли видимость галочки в тёмной теме.
- **Этап 2 формы — сделано:** сгруппированная карточка даты, тумблер «Весь день»,
  строки Начало/Конец с нативными пикерами, повтор и «Завершить» строками (см. выше).
- **Остаётся (TODO, если попросит):**
  - **Повтор «Настроить»** → доп.окно: «Как часто» (по дням/неделям/месяцам/годам) +
    «Кажд. N» + выбор дней недели. Сейчас повтор — готовое меню `RECUR_OPTIONS`.
  - Поля «участники/место/URL/вложение/время в пути/уведомление» — НЕ добавлять (не нужны).

### Боковая панель (как Apple Календарь)
- Проекты: вместо эмодзи — **цветной чекбокс видимости** (`.proj-check`, скруглённый
  квадрат в цвет проекта). Отмечен = проект показан в сетке (день/неделя/месяц), снят =
  скрыт. Состояние — `hiddenLists` (Set СКРЫТЫХ id + `"inbox"`), **локально в localStorage**
  (`planner.hiddenLists`), НЕ в БД: это настройка вида, у каждого устройства своя.
- Фильтр применяется через `hideFilter` ко ВСЕМ источникам сетки: `dayItems`, `monthItems`,
  `weekDays`, и панелям карусели (`dayStaticPane`, `dayPeekPane`, `buildWeekDays`,
  `buildMonth`). Добавляешь новый источник задач в сетку — не забудь `.filter(hideFilter)`.
- Клик по чекбоксу — `toggleListVisible` (stopPropagation), клик по строке — раскрытие задач.
- Области = **заголовки групп** (`.area-group-btn`/`.area-group-name`, серый мелкий текст +
  шеврон), как «iCloud»/«Другие». Без folder-иконки и счётчика. Правка области: десктоп —
  правый клик (`setCtx`), телефон — кнопки `.area-actions` (на десктопе `display:none`).
- «Все задачи» убран. «Завершено»/«Корзина» — переключатели (повторный клик → `filter:"all"`).
- **Перетаскивание проектов** (reorder + смена области): десктоп — потянул мышью,
  тач — долгое нажатие (быстрый горизонтальный свайп остаётся для кнопок «изменить/
  удалить»). `onProjPointerDown` решает swipe/drag/click; `startProjDrag` ведёт живую
  копию; `projDropAt` ищет зону `[data-arealists]` (область id или `""`=свободные) +
  индекс по серединам строк, либо `[data-areahead]` (в начало свёрнутой области);
  `persistProjOrder` пишет `sort_order` группе и `area_id` перетащенному. `sort_order`
  локален в группе (filter по `area_id` сохраняет относительный порядок). FLIP — `projRects`.

### Клавиатура (десктоп)
- Cmd/Ctrl+Z / Shift+Z — undo/redo. Delete/Backspace — удалить выделенное.
- **Shift+стрелки** двигают выделенные: ↑/↓ — ±5 мин, ←/→ — соседний день (с переездом
  вида и сохранением отрезка прокрутки через `keepScrollRef`, выделение «переезжает»).
- **Cmd/Ctrl+C** — копировать выделенные (в `clipRef` + системный буфер как
  `{__planner:1,snaps}`). **Cmd/Ctrl+V** — наш JSON → `pasteSnaps` (дубль в `dateRef` день,
  тот же проект/время); внешний текст → форма создания с этим названием. Перетаскивание
  текста извне в сетку → `onExternalDrop` (задача на времени точки сброса). Копирование
  пока только из сетки дня (выделение); из бокового дерева — TODO, если попросят.

### Автоперенос просроченного (`rollOverdue`, store.js)
- Переносит на сегодня в «весь день» только **задачи** (`!t.is_event && !recurrence &&
  !recurrence_parent && !done && !deleted_at && date<today`). **События не трогаем** — у них
  нет отметки «выполнено», стоят на своём времени, пока не двинут вручную.
- **Это ЕДИНСТВЕННЫЙ автоперенос** — серверной автоматизации (Supabase cron / Edge Function /
  GitHub Action) в репо НЕТ (проверено grep). Если жалуются, что события всё равно уезжают:
  код их исключает (проверено в задеплоенном main), значит виноват клиент со СТАРЫМ кэшем
  (SW network-first обычно даёт свежий, но повторная проверка/жёсткое обновление лечит) либо
  событие реально без `is_event=true`. Не выдумывать второй автоперенос — его нет.

### Строка дат (`.planner-week`, только вид «День») — компактная + КАРУСЕЛЬ
- Компактная (мелкие отступы/шрифт/кружок ~27px).
- **Свайп = карусель из 3 недель** (как сетка дня): `.planner-week-vp` (окно overflow:hidden) →
  `.planner-week-track` (лента, `translateX(-100%)`) → 3 панели `.planner-week`
  (barWeek(date-7), week, barWeek(date+7)). Трек физически едет за пальцем
  (`onWeekBarTouchStart` → `translateX(calc(-100% + dx))`), доводка `weekBarCommit` (до -200%/0,
  на transitionend `shiftWeek` + рецентр) / `weekBarSnapBack`. Рецентр без мигания —
  `weekRecenterRef` + `useLayoutEffect([date])` (трек → -100% ДО отрисовки, новая неделя уже
  в центре). **Десктоп-трекпад** — `onWeekStripWheel` теперь тоже физически тянет трек
  (накопление `weekWheelDxRef`, доводка/возврат по «концу жеста» через 140мс — как onWheel
  карусели дня), а не мгновенный шаг. `shiftWeek` = `setDate(±7)`.
  Тап по дню (малое смещение, не горизонт. свайп) → `onClick` (выбор дня).

## Грабли (на чём уже обжигались)

- **TDZ → белый экран.** Не использовать `dayTl`/`allDay` (и др. `const`, объявленные ниже)
  в массиве зависимостей `useEffect`, размещённого выше по файлу. Падает `ReferenceError`
  при рендере. → размещать обработчик ПОСЛЕ объявления нужных переменных.
- **`overflow:hidden` на `.tl-event`** обрезает тень и чекбокс. Не применять.
- **rafThrottle без flush** → доезд от устаревшей позиции (дёрганье переноса).
- **Серверная перезапись оптимистичного** → прыжки при быстрых правках.
- **Сырой текст ошибки** наружу («Load failed», «AbortError») — переводить через
  `errHint`/`authHint`/`dbHint` (всё на русском).
- **`backdrop-filter` + движение карусели = мелькание.** Стекло на нескольких панелях,
  перерисовываемых при свайпе/зуме, роняет кадры. Снимать blur на время жеста
  (`.swiping`/`.zooming`). Проявлялось как «моргание» при свайпе дней после загрузки.

## Процесс

- Правки → коммит в рабочую ветку → merge в `main` → push (выкатка в прод, GitHub Pages).
- При правке `styles.css`/`main.js` — **бампать `?v=` в `index.html`** (иначе старый кэш).
  Под-модули (Planner.js и пр.) подтягивает SW свежими, но `?v` на entry — страховка.
- **Перед выкаткой логики — реальный рендер-тест** (мок зависимостей + вызов `Planner()`
  в Node), а не только `node --check`: синтаксис не ловит TDZ/рантайм → белый экран.
  **Готовый харнесс** (контейнер эфемерный — пересоздать заново): `mkdir /tmp/rtest && cd
  /tmp/rtest && npm init -y && npm i preact@10.22.0 htm@3.1.1 preact-render-to-string@6`,
  package.json `type:module`. Скопировать `Planner.js`; сделать мок `lib.js` (реальный, но
  `supabase=createClient(...)` заменить на `{}` и убрать `fetchWithTimeout`), мок `store.js`
  (`export function useStore(){return MOCK}` с tasks/lists/areas/user/ready, `StoreProvider`),
  мок `components.js` (заглушки форм). `test.mjs`: глобальные моки `window/document/
  localStorage/ResizeObserver/requestAnimationFrame/performance` (НЕ трогать `navigator` —
  read-only в Node22), затем `render(html\`<${App}/>\`)` из `preact-render-to-string`.
  Прогонять день/неделя/месяц + скрытые проекты. Ловит TDZ и рантайм-ошибки рендера.
- **Проверка иконок/форм «вслепую»** (нет браузера в среде): растеризовать SVG в PNG через
  `npm i sharp` и смотреть через Read (он показывает картинки). Так подобраны форма хвостика
  (имитация стекла: полупрозрачная заливка над цветным блоком) и галочка в чек-листе.
- Видео/скрин от владельца — раскадровывать через ffmpeg (`pip install imageio-ffmpeg`);
  искать моменты diff-ом соседних кадров (numpy), крупные кадры через `-ss T -vf scale=1100`.
- **Версии ассетов сейчас: `styles.css?v=124`, `main.js?v=130`, `Planner.js?v=130`, SW `v3`**.
- **Свайп дня — новая физика:** `runDaySwipe` сглаживает скорость пальца; движок прогнозирует
  конечную позицию на 180мс, выбирает соседний день или возврат и запускает одну CSS-доводку.
  Трекпад использует ту же модель, axis-lock и пиковую скорость. Свайп и зум взаимно блокируются.

## Бэклог идей (из разборов, не реализовано)

- Контекстное меню задачи: «на завтра», «+1 неделя», «дублировать», «убрать дату».
- Подсказка на пустом дне; галочка «Готово» прямо в карточке редактора.
- Напоминания/дедлайны — **крупно и с оговоркой**: на iOS PWA уведомления ненадёжны;
  реалистичный первый шаг — поле «дедлайн» с подсветкой просроченного.
- Поиск: Cmd/Ctrl+K, искать и по заметкам, стрелки+Enter по результатам.
